import { Stack } from "expo-router";

const NotesLayout = () => {
    return (
        <Stack
        screenOptions={{
            headerShown: false
        }}
        ></Stack>
    );
};

export default NotesLayout;
